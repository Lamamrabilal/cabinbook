# ── Secrets / environnement — NE JAMAIS COMMITTER ──────────
.env
.env.local
.env.*.local
*.pem
*.key

# ── Python / Django ─────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Django spécifique
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/

# Migrations générées automatiquement (à adapter : certains projets préfèrent
# les committer — ici on les GARDE car elles définissent le schéma de la BDD)
# apps/*/migrations/*.py
# !apps/*/migrations/__init__.py

# Environnements virtuels
venv/
env/
ENV/
.venv

# ── Tests / couverture ──────────────────────────────────────
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# ── Node / React / Vite ─────────────────────────────────────
node_modules/
frontend-app/dist/
frontend-app/build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# ── Docker ───────────────────────────────────────────────────
# (rien à exclure par défaut — les Dockerfile et docker-compose sont à committer)

# ── Éditeurs / OS ────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp
*.swo
Thumbs.db

# ── Fichiers de scripts temporaires créés pendant le dev ────
seed_data.py
create_slots.py