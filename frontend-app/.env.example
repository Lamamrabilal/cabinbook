# Sentry (monitoring d'erreurs) — laisser vide pour désactiver (dev local).
# Utiliser un projet Sentry distinct de type "React" (DSN différent de celui
# du backend Django), idéalement dans la même organisation hébergée en EU.
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=production
